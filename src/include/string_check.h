#ifndef STRING_CHECK_H_
#define STRING_CHECK_H_

#include "string_check.c"

void temp(void);

int strStr(char* haystack, char* needle);
size_t stringLength(char* current_string, bool with_nullchar);

#endif
